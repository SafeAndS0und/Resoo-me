export enum AvailableElements {
  TOP_FIRSTNAME,
  TOP_LASTNAME,
  TOP_JOB_TITLE,
  TOP_SIZE,
  TOP_BREAKLINE,
  TOP_PLACEMENT,
}

export enum AvailableSections {
  NAME_AND_TITLE,
  CONTACT,
}

export enum AvailableLayouts {
  TOP,
}

export enum AvailableOptions {
  TOP_NAME_BREAKLINE,
  TOP_NAME_SIZE,
}
